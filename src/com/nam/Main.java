/*

    Wtf fucker fuck!!

*/

package com.nam;

public class Main
{
    /** wtfff
     * @author mx
     * @version 42
     * @since 11
     *
     * * <p>Проверяет, допустимый ли ход.</p>
     * <p>Например, чтобы задать ход e2-e4, напишите isValidMove(5,2,5,4);
     * Чтобы записать рокировку, укажите, откуда и куда ходит король.
     * Например, для короткой рокировки чёрных запишите isValidMove(5,8,7,8);</p>
     *
     * @param args Вертикаль, на которой находится фигура (1=a, 8=h)
     * Горизонталь, на которой находится фигура (1...8)
     *    Вертикаль клетки, на которую выполняется ход (1=a, 8=h)
     *   Горизонталь клетки, на которую выполняется ход (1...8)
     * return void, если ход допустим, и false, если недопустим
     *
     * */
    public static void main(String args[])
    {

        int num;

        num = 500;

        System.out.println("Num = " + num);

        num = num * 2;

        System.out.print("Num = ");

        System.out.println(num);

        for (int x = 0; x < 10; x++)
        {
            System.out.println("x = " + x);
        }
    }
}
